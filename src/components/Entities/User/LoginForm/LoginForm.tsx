import React from 'react';
import { LoginFormValues } from 'typings/forms';

type Props = {
    initialValues: LoginFormValues;
};

export const LoginForm: React.FC<Props> = ({ initialValues }) => <>LoginForm</>;
